/**
 * Bump when the privacy policy or terms change in a way that requires
 * re-consent (e.g. new data use, new sub-processor). Onboarding and the
 * Profile consent card compare this against the user's stored
 * consent_privacy_version to decide whether to re-prompt.
 */
export const POLICY_VERSION = '2026-07-07'
