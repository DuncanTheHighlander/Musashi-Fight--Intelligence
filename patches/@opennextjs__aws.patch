diff --git a/dist/build/copyTracedFiles.js b/dist/build/copyTracedFiles.js
index 9701b0401e987ee7cd696873af9d15b751580c52..1e8d4e3f8b44cd5226ddb9db976200856ae57f6a 100644
--- a/dist/build/copyTracedFiles.js
+++ b/dist/build/copyTracedFiles.js	
@@ -225,7 +225,10 @@ File ${serverPath} does not exist
         }
         if (symlink) {
             try {
-                symlinkSync(symlink, to);
+                // Windows: these are always directory links (pnpm package dirs).
+                // Without an explicit type, Node creates a file-type symlink when
+                // the target hasn't been copied yet, which is unreadable as a dir.
+                symlinkSync(symlink, to, "dir");
             }
             catch (e) {
                 if (e.code !== "EEXIST") {
